export interface LightThemeProps {
  children: React.ReactNode;
  mobileappstyle?: boolean;
}

export interface DarkThemeProps {
  children: React.ReactNode;
  mobileappstyle?: boolean;
}

export interface NavbarProps {
  theme: string;
}
